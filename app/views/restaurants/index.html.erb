<h1> All the restaurants </h1>

<% @restaurants.each do |restaurant| %>
  <div><%= link_to restaurant.name, restaurant_path(restaurant) %></div>
<% end %>

<h3><%= link_to "create a new restaurant", new_restaurant_path, class:"btn btn-primary" %></h3>
